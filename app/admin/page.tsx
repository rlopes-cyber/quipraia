"use client";

import { useState } from "react";

const reports = [
  { beach: "Stella Maris", author: "Marina S.", time: "há 18 min", text: "Linha boa no outside. Vento segurando.", status: "Publicado" },
  { beach: "Praia do Flamengo", author: "Caio R.", time: "há 42 min", text: "Mais cheio perto do canal.", status: "Publicado" },
  { beach: "Jaguaribe", author: "Rafael M.", time: "há 1h", text: "Onda pequena, boa para começar.", status: "Aguardando" },
];

export default function AdminPage() {
  const [section, setSection] = useState("Visão geral");
  const [reportState, setReportState] = useState(reports);
  return <main className="admin-shell"><aside className="admin-sidebar"><a className="admin-brand" href="/"><span className="brand-mark">◜</span>ondai</a><span className="admin-label">ADMINISTRAÇÃO</span><nav>{["Visão geral", "Relatos", "Praias", "Anúncios", "Fontes", "Configurações"].map((item) => <button className={section === item ? "selected" : ""} key={item} onClick={() => setSection(item)}>{item}</button>)}</nav><div className="admin-user"><i>R</i><span><b>Ricardo Lopes</b><small>Administrador</small></span></div></aside><section className="admin-main"><header className="admin-header"><div><span className="eyebrow">ONDAI · BETA SALVADOR</span><h1>{section}</h1></div><a className="view-site" href="/">Ver site ↗</a></header>{section === "Visão geral" && <><div className="admin-stats"><article><span>USUÁRIOS</span><b>0</b><small>beta ainda não iniciado</small></article><article><span>RELATOS HOJE</span><b>2</b><small><em>+2</em> últimos 60 min</small></article><article><span>PRAIAS ATIVAS</span><b>4</b><small>Salvador · litoral norte</small></article><article><span>STATUS DAS FONTES</span><b className="ok">●</b><small>todas respondendo</small></article></div><div className="admin-grid"><div className="admin-panel"><div className="panel-head"><div><span className="eyebrow">ATIVIDADE RECENTE</span><h2>Relatos para revisar</h2></div><button onClick={() => setSection("Relatos")}>Ver todos ↗</button></div>{reportState.map((report, index) => <div className="admin-report" key={`${report.author}-${index}`}><span className="admin-avatar">{report.author[0]}</span><div><b>{report.beach}</b><p>“{report.text}”</p><small>{report.author} · {report.time}</small></div><span className={report.status === "Publicado" ? "status live" : "status waiting"}>{report.status}</span></div>)}</div><div className="admin-panel source-panel"><div className="panel-head"><div><span className="eyebrow">INTEGRAÇÕES</span><h2>Saúde dos dados</h2></div></div><div className="source-row"><span><i className="source-dot" />Open-Meteo</span><b>Atualizado</b></div><div className="source-row"><span><i className="source-dot" />DHN / Marinha</span><b>Atualizado</b></div><div className="source-row"><span><i className="source-dot" />Mapa</span><b>Atualizado</b></div><p className="source-tip">O Ondai mostra o horário e a fonte em cada leitura para manter a confiança da comunidade.</p></div></div></>}{section === "Relatos" && <div className="admin-panel full-panel"><div className="panel-head"><div><span className="eyebrow">MODERAÇÃO</span><h2>Relatos da comunidade</h2></div><button className="admin-filter">Filtrar　≡</button></div>{reportState.map((report, index) => <div className="moderation-row" key={`${report.author}-${index}`}><div><b>{report.beach}</b><p>“{report.text}”</p><small>{report.author} · {report.time}</small></div><span className={report.status === "Publicado" ? "status live" : "status waiting"}>{report.status}</span><div className="moderation-actions">{report.status === "Aguardando" ? <button onClick={() => setReportState(reportState.map((item, i) => i === index ? { ...item, status: "Publicado" } : item))}>Aprovar</button> : <button className="muted-action">Ocultar</button>}<button className="deny" onClick={() => setReportState(reportState.filter((_, i) => i !== index))}>Excluir</button></div></div>)}</div>}{section !== "Visão geral" && section !== "Relatos" && <div className="admin-panel empty-panel"><span className="empty-symbol">◌</span><h2>{section} será configurado nesta etapa</h2><p>A estrutura administrativa já está preparada. Aqui entrarão os registros, filtros e ações de operação do beta.</p></div>}</section></main>;
}
